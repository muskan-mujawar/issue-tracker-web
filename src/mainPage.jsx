import React, { useState } from "react";
import { useParams } from "react-router-dom";

const { id } = useParams;

function MainPage() {
  return (
    <div>
      <p>hellos</p>
    </div>
  );
}

export default MainPage;
